# Compiled extension
*.so
*.o
*.bc

# Build artifacts
pg_zvec.index.bc

# zvec is managed separately (as a submodule or separate build)

# pg_regress output (test/results/ and diffs are generated, not committed)
test/results/
test/regression.diffs
test/regression.out
